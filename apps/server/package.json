{
  "name": "server",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "vitest run src",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:",
    "@effect/vitest": "catalog:",
    "@tether/contracts": "workspace:*",
    "@types/bun": "catalog:",
    "effect": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
